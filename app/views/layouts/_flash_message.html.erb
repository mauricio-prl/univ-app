<% flash.each do |type, message| %>
  <div class="ui <%= alert_classes[type] %> message transitions">
    <div class="header">
      <%= message %>
    </div>
    <i class="close icon"></i>
  </div>
<% end %>
